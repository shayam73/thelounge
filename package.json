{
  "name": "thelounge",
  "version": "1.0.0",
  "description": "My Lounge Web App",
  "main": "dist/server/index.js",
  "scripts": {
    "dev": "yarn run build && node ./dist/server/index.js",
    "build": "yarn install && yarn run compile",
    "start": "node ./dist/server/index.js"
  },
  "engines": {
    "node": ">=18.x"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "@types/node": "^22.0.0"
  }
}
